<%= form_with(model: fruit) do |form| %>
  <% if fruit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fruit.errors.count, "error") %> prohibited this fruit from being saved:</h2>

      <ul>
        <% fruit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :item, style: "display: block" %>
    <%= form.text_field :item %>
  </div>

  <div>
    <%= form.label :best, style: "display: block" %>
    <%= form.text_field :best %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
